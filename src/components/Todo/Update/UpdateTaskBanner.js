import React from "react";
import Banner from "../../Generic/Banner";

export default function UpdateTaskBanner({className}) {
    return(
        <Banner className={className} title={'Edit Task'}></Banner>
    );
}