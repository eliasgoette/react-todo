import React from "react";
import Banner from "../../Generic/Banner";

export default function UpdateListBanner({className}) {
    return(
        <Banner className={className} title={'Edit List'}></Banner>
    );
}